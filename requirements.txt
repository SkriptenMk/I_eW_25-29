# Für die Ausführung der Jupyter-Notebooks durch Quarto (lokal und in der GH Action).
# Bei jeder Änderung: `pip install -r requirements.txt` in der .venv ausführen.

# Jupyter-Engine (wird von Quarto zum Ausführen der .ipynb-Dateien benötigt)
jupyter==1.1.1
ipykernel==7.2.0
jupyter_client==8.8.0
jupyter_core==5.9.1
nbclient==0.10.4
nbformat==5.10.4
nbconvert==7.17.1

# In den Notebooks importierte Drittanbieter-Pakete
pytamaro==2.0.1
matplotlib==3.10.9
numpy==2.4.6
pillow==11.3.0
